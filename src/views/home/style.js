import styled from "styled-components";

export const HomeWrapper = styled.div`
  // > 表示后代元素的类
  > .content {
    max-width: 1080px;
    margin-left: auto;
    margin-right: auto;

    .good-price {
      margin-top: 30px;
    }

    .high-score {
      margin-top: 30px;
    }
  }
`;
